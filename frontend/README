nodejs构建前端界面

npm init

安装angular,bootstrap
npm i  angular@1.2.28 --save
npm i  bootstrap@3.3.6 --save


package.json文件会被变

  "dependencies": {
    "angular": "^1.2.28",
    "bootstrap": "^3.3.6"
  }

在当前目录下，生成了目录:node_modules/angular,node_modules/bootstrap,其中所包含的js以及css文件正是相应版本的css文件

增加webpack,gulp支持

"scripts": {
    "build": "webpack",
    "dev": "gulp watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-watch": "^4.3.6",
    "path": "^0.12.7",
    "webpack": "^1.13.0",
    "webpack-dev-server": "^1.14.1",
    "webpack-stream": "^3.2.0"
  }

  //spinexample.js:2 Uncaught ReferenceError: require is not defined
  npm install -g browserify

  alias cnpm="npm --registry=https://registry.npm.taobao.org \
  --cache=$HOME/.npm/.cache/cnpm \
  --disturl=https://npm.taobao.org/dist \
  --userconfig=$HOME/.cnpmrc"


  http://npm.taobao.org/

  cnpm install -g browserify


  https://www.bootcdn.cn/spin.js/